// This is a best practice - value points to exact string
// this way if we're reusing the same action, we know that there
// won't be any typos

export const UserActionTypes = {
    SET_CURRENT_USER: 'SET_CURRENT_USER'
}